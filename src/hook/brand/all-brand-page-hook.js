// Temporary hook - Brand functionality removed
const AllBrandHook = () => {
  const brand = { data: [] };
  const loading = false;
  const pageCount = 0;
  const getPage = () => {};

  return [brand, loading, pageCount, getPage];
};

export default AllBrandHook;
